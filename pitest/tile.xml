<project>
    <properties>
        <highwheel-maven-plugin.version>@highwheel-maven-plugin.version@</highwheel-maven-plugin.version>
        <pitest-maven-plugin.version>@pitest-maven-plugin.version@</pitest-maven-plugin.version>
        <pitest.coverage>@pitest.coverage@</pitest.coverage>
        <pitest.mutation>@pitest.mutation@</pitest.mutation>
        <pitest.skip>@pitest.skip@</pitest.skip>
        <pitest.failWhenNoMutations>@pitest.failWhenNoMutations@</pitest.failWhenNoMutations>
    </properties>
    <profiles>
        <profile>
            <id>jdk8</id>
            <activation>
                <jdk>1.8</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>highwheel-maven</artifactId>
                        <version>${highwheel-maven-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>verify</phase>
                                <goals>
                                    <goal>analyse</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin><!-- highwheel-maven -->
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>${pitest-maven-plugin.version}</version>
                        <executions>
                            <execution>
                                <phase>verify</phase>
                                <goals>
                                    <goal>mutationCoverage</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <skip>${pitest.skip}</skip>
                            <timestampedReports>false</timestampedReports>
                            <excludedClasses>*.Immutable*</excludedClasses>
                            <coverageThreshold>${pitest.coverage}</coverageThreshold>
                            <mutationThreshold>${pitest.mutation}</mutationThreshold>
                            <failWhenNoMutations>${pitest.failWhenNoMutations}</failWhenNoMutations>
                        </configuration>
                    </plugin><!-- pitest-maven -->
                </plugins>
            </build>
            <reporting>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>${pitest-maven-plugin.version}</version>
                        <reportSets>
                            <reportSet>
                                <reports>
                                    <report>report</report>
                                </reports>
                            </reportSet>
                        </reportSets>
                    </plugin><!-- pitest-maven -->
                </plugins>
            </reporting>
        </profile>
    </profiles>
</project>
